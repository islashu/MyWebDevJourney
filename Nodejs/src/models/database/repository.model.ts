export interface RepositoryProps {
    // save(data: any): Promise<any>;
    // update(uuid: string, data: any): Promise<any>;
    // delete(uuid: string): Promise<any>;
    // findAllTabs(uuid: string): Promise<any>;
}
