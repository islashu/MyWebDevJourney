export const POSTS_INFINITE_SCROLL_PAGINATION_RESULTS = 2;

// Controls the number of nested comments that can possibly be rendered. if 1, means only 1 level of nested tabs
export const MAX_TAB_NESTED_COUNT = 1;
