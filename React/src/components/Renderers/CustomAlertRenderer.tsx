import React from 'react';

const CustomAlertRenderer = () => {
    // WIP
    // Note that we need to consider all cases such as the following: alert, info, warning, error, success and etc. and check for each cases before showing the alert

    return <div></div>;
};

export default CustomAlertRenderer;
